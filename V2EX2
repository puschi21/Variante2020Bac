#include <iostream>
#include <cstring>
using namespace std;

int main()
{
    char s[100];
    cin.get(s,100);
    for(int i=0;i<strlen(s);i++)
    {
        if(strchr("-",s[i]))
        {
            strcpy(s+i,s+i+1);
            for(int i;i<strlen(s);i++)
            {
                if(strchr("0123456789,",s[i]))

            }
        }
    }
    cout<<s;
    return 0;
}
